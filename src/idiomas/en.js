const lenguaje = () => { return 'en' } //Ingles

const gemini = () => { return `『🪐』It seems that the coronavirus is not a problem for us.\n\n• 𝙋𝙤𝙧 𝙚𝙟𝙚𝙢𝙥𝙡𝙤:\n` }
const geminierror = () => { return `『📣』It is always a good time for us to be patient, we always take care of you.` } 

export default { lenguaje, gemini, geminierror }
